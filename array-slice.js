function arrSlice() {
  let kota = ["Jakarta", "Surabaya", "Bandung", "Semarang", "Yogyakarta"];
  console.log(kota.slice(1));
  console.log(kota.slice(2, 4));
  console.log(kota.slice(-3));
}

arrSlice();
